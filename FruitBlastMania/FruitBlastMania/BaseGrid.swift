/*
    BaseGrid is the base grid class that all other grids (different number of sections, have obstacles etc.) 
    should inherit from. This is where we should add attributes whereby we want all grids to have although
    there is currently none but we should keep it open for extension.
*/

import Foundation

class BaseGrid {
    
    // Used to store things that all grids should have as a base (nothing at the moment but this allows
    // for extensibility in the future)
}
